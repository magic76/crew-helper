# AndroidX is required by modern Android dependencies such as OkHttp 5's
# startup and annotation artifacts. Jetifier stays disabled; Crew Helper does
# not rely on legacy support-library rewriting.
android.useAndroidX=true
